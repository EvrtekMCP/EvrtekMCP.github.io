'use strict';
// Single source of truth for the version string. The cache-busting ?v= stamps
// on every script and stylesheet in index.html must match this on release —
// the Arcade's standing rule, orders/arcade-releases.md.
var CW_VERSION = '0.18.0';
